# Production defaults (loaded when running `pnpm build` / `pnpm start` or on Vercel)
# Set RESEND_API_KEY, CONTACT_EMAIL (and optionally FROM_EMAIL) in your host's env or .env.production.local.

FROM_NAME=Kalekye Contact
