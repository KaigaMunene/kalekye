# Development defaults (loaded when running `pnpm dev`)
# Add secrets in .env.local (gitignored) — copy from .env.example and fill in.

FROM_EMAIL=onboarding@resend.dev
FROM_NAME=Kalekye Contact (Dev)
